name: Deploy Frontend
on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'public/**'
      - 'package.json'
      - 'vite.config.ts'
      - 'index.html'
      - 'tailwind.config.ts'
      - 'tsconfig.json'

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build
        env:
          VITE_API_baseurl: https://backend.lafftale.online
          VITE_API_weburl: https://backend.lafftale.online
          VITE_ASSETS_weburl: https://lafftale.online

      - name: Deploy to production
        run: rsync -av --delete dist/ /home/lafftale/htdocs/lafftale.online/

      - name: Deployment complete
        run: echo "✅ Frontend deployed to lafftale.online"
