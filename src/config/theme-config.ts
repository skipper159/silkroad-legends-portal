import { TemplateDefinition } from '../lib/template-system/types';

// ============================================================================
// TEMPLATE CONFIGURATION
// ============================================================================
// Switch the active template by uncommenting the desired import below.
// A server restart/rebuild is required after changing this file.

// Option 1: Default Legacy Template (Original Design)
// import ActiveTemplate from '../templates/default';

// Option 2: Modern V2 Template (Sidebar Layout, Dark Theme)
import ActiveTemplate from '../templates/modern-v2';

//Config Export
export default ActiveTemplate;
